{
  "name": "@pkg/common",
  "description": "Generic util library",
  "version": "1.0.0",
  "private": true,
  "license": "ISC",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "lint": "eslint --ext .js,.ts 'src/**/*.ts' --fix",
    "repl": "ts-node-dev --respawn src/repl.ts"
  },
  "dependencies": {
    "papaparse": "^5.3.0"
  },
  "devDependencies": {
    "@types/node": "*",
    "@types/papaparse": "^5.2.4"
  },
  "engines": {
    "node": ">=12 <15"
  }
}
