import { RouteMeta } from '../../../../../types'
import ParentMeta from '../../meta'

const slug = 'scroll-restore-test'
const routeMeta: RouteMeta = {
  title: 'Scroll Restore Test',
  slug,
  path: `${ParentMeta.path}/${slug}`,
}
export default routeMeta
